const { Favorites, Product } = require('../../schemas')

const removeFavoriteById = (req, res) => {
    const { _id } = req.params
    try {
        const { User_id } = req.locals


    } catch (error) {

    }
}

module.exports = removeFavoriteById